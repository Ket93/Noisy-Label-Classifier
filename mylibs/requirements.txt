# Install PyTorch with CUDA support — default `pip install torch` gives CPU-only.
# Find your CUDA version with `nvidia-smi`, then run the appropriate command from:
#   https://pytorch.org/get-started/locally/
# Example for CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
torchvision>=0.15
numpy>=1.24
Pillow>=9.0
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
flask>=3.0
